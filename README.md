# weather-app
A real-time weather web application built using Flask, HTML, and CSS for the frontend, and Python for backend development. The app uses the OpenWeather API to fetch live weather data based on user input and displays temperature and weather conditions in a clean and responsive interface.
